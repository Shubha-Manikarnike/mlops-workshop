name: eda
conda_env: conda.yml

entry_points:
  main:
    parameters:
      input_artifact:
        description: input_artifact
        type: string
        
    command: >-
        python run.py  --input_artifact {input_artifact}
